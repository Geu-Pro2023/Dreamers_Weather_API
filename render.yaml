services:
  - type: web
    name: dreamers-weather-api
    env: docker            # Use Docker environment
    dockerfilePath: Dockerfile
    startCommand: uvicorn main:app --host=0.0.0.0 --port=10000
